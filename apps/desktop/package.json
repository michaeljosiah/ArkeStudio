{
  "name": "@arke-studio/desktop",
  "version": "0.2.3",
  "private": true,
  "description": "Electron desktop shell: embeds the coordinator in main, supervises OpenCode and Voxa, and serves the React client as one app (SPEC-001 §2.2).",
  "type": "module",
  "main": "dist/main.cjs",
  "scripts": {
    "build": "npm run build --workspace @arke-studio/client && node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "rebuild:native": "node scripts/rebuild-native.mjs",
    "start": "npm run build && npm run rebuild:native && electron dist/main.cjs",
    "package": "npm run verify:licenses && npm run build && npm run rebuild:native && electron-builder --publish never",
    "release": "npm run verify:licenses && npm run build && npm run rebuild:native && electron-builder --publish always",
    "verify:licenses": "node scripts/verify-licenses.mjs"
  },
  "dependencies": {
    "better-sqlite3-electron": "npm:better-sqlite3@^12.11.1",
    "electron-updater": "^6.8.9"
  },
  "devDependencies": {
    "@arke-studio/adapter-opencode": "*",
    "@arke-studio/contracts": "*",
    "@arke-studio/coordinator": "*",
    "@arke-studio/providers": "*",
    "@arke-studio/voice": "*",
    "@types/node": "^22.0.0",
    "electron": "^43.2.0",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.25.12",
    "typescript": "^5.6.0"
  }
}
