{
  "name": "@arke-studio/adapter-opencode",
  "version": "0.1.0",
  "private": true,
  "description": "The harness adapter for OpenCode v2 headless. In SPEC-001 it ships as the contract plus a scripted mock; SPEC-005 adds the live server backing with OpenAPI capability probing.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "@arke-studio/contracts": "*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
