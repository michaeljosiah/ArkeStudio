{
  "name": "@arke-studio/adapter-claude",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "description": "The bring-your-own Claude Code harness. Discovery over the user's own installation, plus the behavioural confinement probe that decides whether it may be offered at all.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.235",
    "@arke-studio/contracts": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
