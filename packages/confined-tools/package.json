{
  "name": "@arke-studio/confined-tools",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "description": "Application-owned file and world-query tools confined to a pinned session directory, shared by the harnesses that execute tools themselves.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test \"test/**/*.test.ts\""
  },
  "dependencies": { "@arke-studio/contracts": "*" },
  "devDependencies": { "@types/node": "^22.0.0", "tsx": "^4.19.0", "typescript": "^5.6.0" }
}
