{
  "name": "@arke-studio/providers",
  "version": "0.1.0",
  "private": true,
  "description": "Typed skeleton for the provider layer — FAL, Higgsfield, ElevenLabs, OpenAI, Anthropic, Ollama. Implementations arrive with SPEC-008/SPEC-009; the workspace graph is settled here (SPEC-001 D9).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@arke-studio/contracts": "*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
