{
  "name": "@arke-studio/contracts",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "description": "Schema-first domain contracts: the world entity model, proposals, jobs, domain events, transport frames, and the harness adapter interface (master spec §2, §3, §10; SPEC-001 R-2).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "zod": "^3.23.8",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/three": "^0.185.4"
  }
}
