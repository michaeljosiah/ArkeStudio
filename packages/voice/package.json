{
  "name": "@arke-studio/voice",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "description": "Typed Voxa sidecar client and voice catalogue for local TTS/STT, with protocol validation and runtime readiness handling (SPEC-011).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "@arke-studio/contracts": "*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "tsx": "^4.19.2"
  }
}
