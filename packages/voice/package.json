{
  "name": "@arke-studio/voice",
  "version": "0.1.0",
  "private": true,
  "description": "Typed skeleton for the Voxa voice sidecar — local TTS/STT, supervised as a child process. The protocol client and catalogue arrive with SPEC-011; the workspace graph is settled here (SPEC-001 D9).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@arke-studio/contracts": "*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
