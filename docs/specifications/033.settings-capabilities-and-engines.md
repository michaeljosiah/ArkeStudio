---
specId: SPEC-033
slug: settings-capabilities-and-engines
title: Settings speaks in capabilities — Local AI, Cloud AI, and the machinery beneath
status: draft
owner: core-maintainers
branch: claude/settings-capabilities
sourceOfTruth: filesystem
parent: docs/specification.md
depends: [SPEC-001, SPEC-004, SPEC-008, SPEC-009, SPEC-016, SPEC-021, SPEC-022, SPEC-028, SPEC-030, SPEC-032]
amends: [SPEC-008, SPEC-028]
created: 2026-08-27
updated: 2026-08-27
---

<!-- A person opening Settings is asking what they can make. The screen answers with our parts list. -->

# 1 · Requirements

## 1.1 Summary

Settings has nine tabs, and one of them — Local runtime — is a master and detail of six more:
`This machine · Components · Voice · ComfyUI · Local models · Authoring harness`. Those six are
three different kinds of thing in one rail: what the machine is, what can be downloaded, and
which runtimes host it. Read together they answer *what is installed here*, which is a question
about our architecture. They do not answer *what can I make here*, which is the question the
person who opened the panel has.

**SPEC-028 already said so.** Its R-1 requires Settings to be organized by capability rather than
by runtime; its §2.3 draws the layout; its T-24 is *replace the Local runtime rail with
capability-first groups*, and T-24 is open. This specification is not a new idea arriving beside
that one. It is the one that gets built, and it revises SPEC-028's answer in four places, each
recorded as an amendment with its reason (§2.10).

Three facts in the code say the taxonomy is the fault, not the drawing.

`statedElsewhere()` suppresses a setup component from Components when the same component is
already stated under Providers, Voice, ComfyUI or Local models — hand-written deduplication,
conditional per destination, for groups that overlap by construction.

The authoring harness is stated twice: as a top-level tab, and as a group inside Local runtime.

And model identifiers carry their runtime — `ollama-gemma4-12b`, `ollama-runtime` — so a screen
that lists models by id lists engines whether it means to or not.

This specification inverts the hierarchy. It runs today as
`components → engines → models → capabilities`. It becomes
`capabilities → models → engines → components`, and most people stop at models.

It replaces one screen with two and moves the machinery behind both:

**Local AI** answers *what can this machine run, and what is on it*. Five capability rows, the
local models under each, and one state word per model.

**Cloud AI** answers *which remote model runs each capability*. It is today's "Who does what",
renamed, made cloud-only, and spoken in a creator's words rather than ours.

**Engines** answers *what is running underneath* — ComfyUI, Ollama, Voxa — unabridged, for the
sessions where that is the question.

Two boundaries are load-bearing and are stated as requirements rather than left to taste. **No
cloud provider appears on Local AI, and no local model appears on Cloud AI.** And **neither
screen routes between the two**: where a piece of work runs is a production's decision, made at
dispatch.

The substantive contract change is smaller than the surface change. Fit — *can this machine run
it* — becomes an axis of its own, separate from activation, which is what SPEC-028 R-6's closed
vocabulary already describes. Today one three-word vocabulary carries both questions in two
different places, which is why a screen reading both must decide by hand which to believe.

## 1.2 Scope

**In scope**

- The five settings surfaces, and the rule that assigns every fact to exactly one of them.
- **Fit**: what this machine can run, as a verdict with four outcomes and a preserved `unknown`.
- **Activation**: SPEC-028 R-6's closed vocabulary, adopted unchanged and made orthogonal to fit.
- The state a model row shows, derived from fit and activation together.
- The recommendation: authored for quality, filtered by measurement.
- Local AI: the five capability rows, the machine header, and the refusals.
- Cloud AI: the rename, the cloud-only filter, the shared capability vocabulary, and what leaves.
- Engines: the machinery screen, and the deletion of the suppression that preceded it.
- What a declared install chain adds to SPEC-028 R-5's dependency closure.
- One Downloads surface for everything in flight.
- Where work runs: the local-or-cloud choice, per capability, per production.
- The language rule that governs every string on all three screens.
- Amendments to SPEC-008 R-20, R-22 and to SPEC-028 R-1, R-2, R-3, R-4, R-6, R-22.

**Out of scope**

- The accept gate — SPEC-004, untouched. Nothing here writes to a world.
- Recipe readiness and the ComfyUI engine lifecycle — SPEC-021 §2.12, SPEC-028 R-19..R-23,
  consumed unchanged but for R-22's first clause. This specification composes `RecipeReadiness`;
  it does not re-derive or replace it.
- Voxa's boundary, its process model and its speech plane — SPEC-028 R-7..R-12, unchanged. Voxa
  gains a row on Engines and loses nothing.
- The dependency-closure transaction itself — SPEC-028 R-5 and §2.4, unchanged. §1.7 adds what
  the closure must **declare** and how its size is **stated**, not how it runs.
- The queue, dispatch admission, and cost capture — SPEC-009, unchanged.
- The finding contract, its severities, its control registry and its view — SPEC-032. R-40 and
  R-56 are consumers of it, not authors of a second one.
- Providers: which services are connected, and the keys. SPEC-008 §2.4 and SPEC-028 R-33,
  unchanged. Cloud AI references a provider's connection state and never edits it.
- First run. It gains no setup chooser — D10, with its reason.
- The model manifest's contents. This specification adds fields to a model row; it does not
  curate models.

## 1.3 The five surfaces (R-1..R-8)

- **R-1** Settings SHALL present five surfaces answering five distinct questions, and every fact
  about providers, models, runtimes or components SHALL belong to exactly one of them:

  | Surface | Question |
  | --- | --- |
  | Providers | Which services are connected? |
  | Cloud AI | Which remote model runs each capability? |
  | Local AI | What can this machine run, and what is on it? |
  | Engines | Which local services are running, and how? |
  | Harness | Which harness runs the authoring agents, and how? |

  This carries SPEC-028 R-1 forward and extends it: R-1 organized *Local runtime* by capability,
  and this organizes the whole panel.

- **R-2** No cloud provider SHALL appear on Local AI, in any state, including disabled or
  unavailable.

- **R-3** No local model SHALL appear on Cloud AI, in any state, including disabled or
  unavailable.

- **R-4** Neither Local AI nor Cloud AI SHALL offer any control that selects between local and
  cloud execution.

- **R-5** The authoring harness SHALL be stated on exactly one surface. The group inside Local
  runtime is removed; the Harness tab remains.

- **R-6** `statedElsewhere()` and its table SHALL be **deleted**, not relocated or rewritten.
  - **WHEN** the Engines screen has landed and any suppression of a fact on account of its being
    stated elsewhere is still required **THEN** the assignment made by R-1 is wrong somewhere,
    and that is the finding.

- **R-7** Where a surface names a fact owned by another, it SHALL reference the owning surface's
  control rather than reproducing the control.
  - **WHEN** Cloud AI shows a model whose provider has no key **THEN** it states the provider's
    connection state and offers the route to Providers, and does not accept a key.

- **R-8** The rail SHALL read: Providers · Cloud AI · Local AI · Engines · Harness · Appearance ·
  Notifications · Sign-in · Sample world · About.

## 1.4 Fit — what this machine can run (R-9..R-18)

- **R-9** A **fit verdict** SHALL be a property of a model and a machine alone. It SHALL NOT
  depend on whether the model is installed, whether its engine is running, or whether anyone
  turned it off.

- **R-10** Fit SHALL have five outcomes: `runs-well`, `runs-slowly`, `insufficient`,
  `unsupported`, `unknown`.

- **R-11** `unsupported` SHALL be **declared**, never inferred from a quantity. It follows from a
  model declaring a platform or accelerator the machine does not have.
  - **WHEN** a model declares `accelerator: ["cuda"]` and the machine reports none **THEN** the
    verdict is `unsupported`, whatever its VRAM figure.
  - This amends SPEC-028 R-22, whose first clause reads a VRAM shortfall as `unsupported`.

- **R-12** `insufficient` SHALL be **measured**: a declared floor — VRAM, system memory, free
  disk — that the machine does not meet. It carries SPEC-028 R-22's *both figures* requirement
  unchanged; only the word changes.

- **R-13** `runs-slowly` SHALL be measured against a stated headroom margin over the declared
  floor, and the margin SHALL be **25 percent for VRAM and for system memory**.
  - Every floor met with at least the margin → `runs-well`.
  - Every floor met, at least one of VRAM or system memory below the margin → `runs-slowly`.
  - Free disk SHALL NOT contribute to `runs-slowly`. Disk decides whether a model can be
    installed, not how fast it runs once it is.

- **R-14** A failed probe SHALL yield `unknown` and SHALL NOT yield any refusing verdict. This
  restates SPEC-008 D12, SPEC-021 D15 and SPEC-028 R-22's third clause, and amends none of them.

- **R-15** Fit SHALL remain distinct from the **free-memory** check SPEC-028 R-22 governs. Total
  suitability is a fit verdict; a device with sufficient total VRAM and too little free at this
  moment is a dispatch-time refusal, and that path — unload once, re-measure, name the remaining
  shortfall — is unchanged.

- **R-16** `insufficient` and `unsupported` SHALL remain distinct verdicts with distinct
  remedies. `insufficient` offers the smaller models for that capability; `unsupported` offers
  nothing, and says so.

- **R-17** Hardware probing SHALL be stated as unmeasured until it has run, and SHALL NOT be
  reported as zero, absent, or unavailable.

- **R-18** The headroom margin, the probed fields, and the declared requirement fields SHALL be
  defined in contracts and consumed identically by every reader. Two conforming builds SHALL NOT
  disagree about the same machine.

## 1.5 Activation, and the state a row shows (R-19..R-26)

- **R-19** **Activation** SHALL use SPEC-028 R-6's closed vocabulary, adopted unchanged:
  `not-installed`, `downloading`, `installing`, `starting`, `ready`, `needs-attention`,
  `unsupported`, `unknown`.

- **R-20** Activation and fit SHALL be **orthogonal axes**, and `unsupported` and `unknown` SHALL
  be read from fit alone.
  - R-6's vocabulary carried two questions in one word: `downloading` is a fact about a transfer,
    and `unsupported` is a fact about the machine. A model can be `downloading` on a machine that
    cannot run it, and today there is no word for that.

- **R-21** A model row SHALL show one headline state derived from both axes:

  | Fit | Activation | Row shows |
  | --- | --- | --- |
  | `insufficient` or `unsupported` | any | **Unsupported** |
  | `runs-well`, `runs-slowly`, `unknown` | `ready` | **Installed** |
  | `runs-well`, `runs-slowly`, `unknown` | `not-installed` | **Available** |
  | `runs-well`, `runs-slowly`, `unknown` | in flight or `needs-attention` | the activation state |

- **R-22** A row in the **Unsupported** state SHALL carry the reason that distinguishes which
  verdict produced it (R-16), so that one label never costs the two verdicts.

- **R-23** A model whose fit is `unknown` SHALL be offered, not withheld. The row states that
  the machine has not been measured.

- **R-24** A model in the **Unsupported** state SHALL NOT offer install.

- **R-25** A model that is **Installed** but not dispatchable SHALL say so from its engine's
  readiness (SPEC-021 `RecipeReadiness`), and SHALL NOT restate that readiness as a fit or
  activation fact.

- **R-26** A model turned off by the person, in Providers, SHALL be shown in that state and
  SHALL NOT be reclassified as unsupported, unavailable, or missing. Being switched off is a
  decision, and the other three are conditions.

## 1.6 The recommendation (R-27..R-31)

- **R-27** The manifest SHALL carry a **preference order per capability**, authored, expressing
  quality alone and making no claim about any machine.

- **R-28** The recommendation for a capability SHALL be the first entry in that order whose fit
  verdict is `runs-well` on this machine.

- **R-29** Nothing whose fit is `unknown` SHALL be recommended. A recommendation is a claim about
  the machine, and no claim can rest on an unmeasured one.

- **R-30** A capability with no `runs-well` entry SHALL have no recommendation, stated as an
  absence rather than as a fault.

- **R-31** At most one model per capability SHALL be marked recommended.

## 1.7 What the install chain must declare (R-32..R-38)

SPEC-028 R-5 and §2.4 already require one-action activation over a complete dependency closure,
with total transfer, peak disk and hardware compatibility computed before anything downloads.
That transaction is unchanged. This section governs what the closure is **built from** and how it
is **spoken about**.

- **R-32** A model's requirements SHALL be **declared data**, and a closure SHALL NOT be inferred
  from an identifier's prefix or shape.
  - Per-recipe weight components are already derived from the recipe catalogue rather than
    hand-listed. That derivation is the pattern R-32 generalizes; it SHALL NOT be duplicated by a
    second declaration of the same weights.

- **R-33** The size stated before an install SHALL be the closure's total, not the model's own
  weights.

- **R-34** A closure with more than the model in it SHALL be stated **by count** —
  `three supporting components` — and the components themselves SHALL be reachable behind an
  Advanced disclosure and SHALL NOT be named in the primary line.

- **R-35** Free disk SHALL be checked against the whole closure, per destination volume, and the
  guard SHALL state its evidence. This restates the existing per-volume behaviour and does not
  amend it.

- **R-36** Removing a model SHALL be offered wherever a size on disk is stated, and SHALL state
  what goes and what it frees.

- **R-37** Two models sharing a component SHALL NOT install it twice, and removing one SHALL NOT
  remove a component the other still requires.

- **R-38** A cancelled or failed install SHALL leave nothing behind. A fragment that survives its
  fetch SHALL be reported, not silently retained.

## 1.8 Local AI (R-39..R-49)

- **R-39** Local AI SHALL replace Local runtime, at `/settings/local-ai`. It supersedes SPEC-028
  T-24.

- **R-40** It SHALL present exactly five capability rows, always all five, in the order
  **Images · Video · Voice · Music · Language**.
  - This amends SPEC-028 R-1, R-2 and R-3, whose groups are **Speech** and **Local generation**
    with rows *Local voices*, *Dictation*, *Conversational voice*, *Local images*, *Local video*
    and *Cloned voices*. §2.10 carries the reason.

- **R-41** The Voice row SHALL state local voices, dictation and conversational voice as
  **independently readable lines**, and SHALL NOT collapse them into one state.
  - This preserves SPEC-028 R-2 exactly. **WHEN** Kokoro is unavailable and whisper.cpp is ready
    **THEN** dictation still reads as usable.
  - Cloned voices is a model under Voice, not a row beside it.

- **R-42** Language SHALL be a row. SPEC-028's layout had none, and the writing models are the
  local capability most people reach first.

- **R-43** A capability with no local model in the manifest SHALL show its row and state the
  absence. It SHALL NOT be hidden.
  - **WHEN** no local provider declares `music` **THEN** the Music row states that there are no
    local music models, and is not a fault.

- **R-44** A model entry SHALL state its display name, its row state, its fit verdict and its
  size. Quantisation, context length, backend, engine and install location SHALL sit behind an
  expandable detail.

- **R-45** A model entry SHALL NOT be headed, named or grouped by its engine. Which engine serves
  it is a fact of the Engines screen, available in the detail.

- **R-46** The screen SHALL carry a header stating the machine in the figures the verdicts turn
  on — accelerator, VRAM, free disk — or stating that they have not been measured (R-17).

- **R-47** Hardware detection SHALL be triggered by opening this screen where it has not already
  run, and the header SHALL distinguish *not yet measured* from *measured and failed*.

- **R-48** Where a capability cannot run locally, the primary statement SHALL be about the
  **capability**, with the engine's own words behind a technical-details control.
  - **WHEN** the video engine has failed **THEN** the row states that local video is
    unavailable, then that the video engine could not start, and the engine's message is one
    control away.

- **R-49** The join behind R-48 SHALL be SPEC-032's finding contract. Local AI SHALL NOT compute
  a second join between engines and capabilities.
  - **WHEN** the finding contract is not yet available **THEN** the row states only what it can
    read directly, and invents no cause.

## 1.9 Cloud AI (R-50..R-56)

- **R-50** "Who does what" SHALL be renamed **Cloud AI**, at `/settings/cloud-ai`.

- **R-51** Its model list SHALL be filtered to providers where `local === false`. This amends
  SPEC-008 R-20: a routing default is a concrete **cloud** model.

- **R-52** Capability labels SHALL be creator-facing and SHALL be the same words Local AI uses:
  `Images`, `Video`, `Voice`, `Music`, `Language`. `Clips`, `Frames & stills`, `Score & songs`
  and `Direct LLM work` are retired.

- **R-53** A model SHALL be shown with its provider and that provider's connection state, and the
  remedy for an unconnected provider SHALL be a route to Providers, never a key field here. The
  stored-versus-tested distinction SPEC-028 R-33 requires is displayed, not re-derived.

- **R-54** The existing refusals SHALL survive the rename: a stranded default still distinguishes
  *turned off in Providers* from *routed here, but this provider has no key*, and manifest drift
  keeps its section.

- **R-55** The per-agent model overrides SHALL move to Harness. They assign models to authoring
  agents, which is agent execution, and they admit local models, which R-3 forbids here.

- **R-56** Cloud AI SHALL NOT land while any local capability default remains in force and
  unreachable.
  - **WHEN** the local-or-cloud choice (§1.11) exists **THEN** each local default migrates to it.
  - **WHEN** it does not **THEN** each local default is cleared, and the person is told what was
    cleared and where the choice now lives.

## 1.10 Engines (R-57..R-63)

- **R-57** Engines SHALL be a **surface of its own**, not a collapsed section inside Local AI.
  This amends SPEC-028 R-4, whose *Advanced runtime details* disclosure lives on the capability
  page. §2.10 carries the reason.

- **R-58** It SHALL present one row per local service — ComfyUI, Ollama, Voxa — stating its
  status and what it is used for, in the same five capability words.

- **R-59** An engine's detail SHALL be unabridged, and SHALL carry everything SPEC-028 R-4
  required of Advanced: selected runtime source, version, safe location, model-folder mapping,
  port, executable selection, logs, restart, re-verify and repair.
  - **AND** an absolute executable path SHALL remain host-owned; renderer state carries at most a
    basename and a safe source label. SPEC-028 R-4's constraint is unchanged.

- **R-60** Setup components SHALL be stated under the engine that requires them. A component
  required by no engine SHALL keep a place, and SHALL NOT be the organising idea.

- **R-61** An engine SHALL NOT be presented as a provider, and a provider SHALL NOT be presented
  as an engine. Voxa hosts Kokoro and whisper.cpp; it is one engine and two providers.

- **R-62** The authoring harness SHALL NOT appear on this screen (R-5).

- **R-63** Engines SHALL land with or immediately after Local AI. A release in which Local AI has
  stopped stating a fact and Engines does not yet state it SHALL NOT ship.

## 1.11 Where work runs (R-64..R-69)

- **R-64** The choice between local and cloud SHALL be expressed **per capability, per
  production**, and SHALL be durable.

- **R-65** It SHALL seed the dispatch picker and SHALL NOT lock it. Per-dispatch override
  survives unchanged (SPEC-008 R-21).

- **R-66** A choice that cannot be honoured SHALL be stated at dispatch and SHALL NOT be silently
  substituted.
  - **WHEN** a production prefers local video on a machine where local video is unavailable
    **THEN** the dispatch says so and offers the cloud path, and does not quietly spend.

- **R-67** It SHALL be a production's setting, not an installation's.

- **R-68** No settings surface SHALL edit it (R-4).

- **R-69** Existing local capability defaults SHALL migrate into it, or be cleared with a stated
  reason (R-56).

## 1.12 Downloads (R-70..R-74)

- **R-70** One surface SHALL show everything being fetched, whichever capability or engine
  started it.

- **R-71** It SHALL carry progress, rate, required space, install location, pause, retry and
  remove.

- **R-72** A capability row SHALL keep the download, repair, progress and next action SPEC-028
  T-25 puts on it, and SHALL NOT grow a downloader of its own. Downloads is where work in flight
  is *watched*; the row is where it is *started*.

- **R-73** It SHALL be reachable from Local AI and from Engines, and owned by neither.

- **R-74** A fetch that fails or is cancelled SHALL report what it left behind, if anything
  (R-38).

## 1.13 Language (R-75..R-78)

- **R-75** Every string on these screens SHALL be a label, a count, a state or a refusal. No
  string SHALL explain why a control is arranged as it is, or reassure.

- **R-76** A fit or size statement SHALL be **measured figures**, never adjectives.
  - `Installed · runs well · 14.6 GB` conforms.
  - `Fast · Good reasoning` does not: both are judgements about a model, not states of one.

- **R-77** A refusal SHALL be one clause on the thing refused, carrying its figures (R-12).

- **R-78** The two screens SHALL share one capability vocabulary (R-52). A capability named
  differently on Local AI and Cloud AI is a defect.

# 2 · Design

## 2.1 Files

```
packages/contracts/src/
  settings.ts        fit verdict, probes, the widened model row
  manifest.ts        declared requirements, the per-capability preference order
  runtime-gate.ts    the fit derivation and the recommendation, both pure
  setup.ts           the declared closure, and what Downloads needs
packages/coordinator/src/setup/
  catalogue.ts       requirement graph as data
  local-setup.ts     closure resolution, whole-closure arithmetic, per-volume guard
packages/client/src/screens/
  settings-local-ai.tsx
  settings-cloud-ai.tsx
  settings-engines.tsx
  settings-downloads.tsx
```

## 2.2 Four axes, four owners

The present design has one three-word vocabulary — `ready | disabled | unknown` — doing two
different jobs in two different places. `LocalRuntimeModel.state` uses it for *can this machine
run the model*. `RecipeReadiness.state` uses it for *can this recipe be dispatched right now*.
The words are identical and the questions are not, which is why a screen reading both has to
decide, by hand, which one to believe.

SPEC-028 R-6's eight-state vocabulary is a third answer to a fourth question — *where is this in
its activation* — and it too carries a fit fact inside it, in the word `unsupported`.

There are four independent axes, and this specification gives each exactly one owner:

| Axis | Question | Owner | Vocabulary |
| --- | --- | --- | --- |
| Fit | Can this machine run it? | `runtime-gate.ts` | `runs-well`, `runs-slowly`, `insufficient`, `unsupported`, `unknown` |
| Activation | Where is it in getting here? | the setup ledger, over the declared closure | SPEC-028 R-6, unchanged |
| Readiness | Can it dispatch now? | SPEC-021 `RecipeReadiness` | unchanged |
| Enabled | Did the person turn it off? | Providers | unchanged |

Local AI **composes** these. It derives nothing another owner already answers, which is what
keeps two screens from disagreeing about one model.

The separation fixes a real conflation. A model can be installed and unfit — someone changed
graphics cards. A model can be fit and absent. A model can be downloading onto a machine that
will not run it. Today the first and third have no representation at all, and the second and a
dead engine share the word `disabled`.

## 2.3 Why one identity, not two

A ComfyUI recipe is a manifest model: recipe readiness matches the recipe id against the model
id, and the provider table exists so that recipes reach the same picker, queue and ledger as
every cloud model. That identity is load-bearing here, and this specification does not introduce
a second one. A model row on Local AI is one manifest model, and its readiness, its weights
component and its recipe are the same thing seen from three sides.

## 2.4 Why the duplication across two screens is correct

Local AI and Cloud AI both list Images, Video, Voice, Music and Language. That looks like
repetition and is not, because the two screens answer different questions about the same word.

Cloud AI chooses a remote model. Local AI prepares this machine. A person setting up a laptop for
a flight is doing the second and not the first, and a person deciding what a production should
cost is doing the first and not the second.

The alternative — one capability screen holding both — was considered and rejected (D1). It reads
well in a mock and fails in use, because the two halves have nothing in common past the noun.
Cloud has a key, a price and a rate limit. Local has a size, a fit and a download. Merging them
produces a row that is half greyed out whichever way the person is thinking.

The duplication is the price of a boundary with no exceptions, and a boundary with one exception
is what the current screen already is.

## 2.5 Why `unsupported` is declared and `insufficient` is measured

They lead to different actions, and only one of them is a shopping decision.

`insufficient` says a bigger machine would work, and names the figure that would make it work.
The remedy is smaller models, offered on the spot.

`unsupported` says no machine of this kind will work — the wrong accelerator, the wrong platform.
There is no figure to reach, and offering smaller models would be a lie of omission.

SPEC-028 R-22 currently uses `unsupported` for a VRAM shortfall, which is the more common case
and the one where the word is least true. R-11 and R-12 swap them: the measured shortfall becomes
`insufficient` and keeps both figures, and `unsupported` becomes what it says.

## 2.6 The headroom margin, and why the number lives here

`runs-slowly` cannot be derived from the floors alone: meeting a floor by two hundred megabytes
and meeting it by six gigabytes are the same boolean and very different experiences. Weights are
the floor; activations, the KV cache and the compositor's own framebuffer live above it, and a
machine at the floor spills into system memory and slows by an order of magnitude.

The margin is **25 percent of the declared floor, for VRAM and system memory**. It is an arguable
number, which is exactly why it is written in a specification rather than chosen privately by an
implementer — two conforming builds must not disagree about the same machine (R-18).

Free disk is deliberately excluded. A model with barely enough room to install runs at exactly
the speed of a model with a terabyte spare. Including disk would report a slowness that does not
exist.

This is a judgement about *total* suitability and does not touch the free-VRAM path (R-15): a
machine that fits a model comfortably and happens to be holding another model resident is
answered at dispatch, by unloading once and re-measuring, exactly as SPEC-028 R-22 requires.

## 2.7 Why the recommendation is authored *and* filtered

Two pure approaches were available and both are wrong on their own.

A **derived** recommendation — the largest model that runs well — recommends by size, which is a
proxy for quality and a poor one. It would put a large mediocre model above a small excellent
one, and it would change its mind when someone plugs in a monitor.

An **authored per-machine** flag — this model is recommended for 12 GB cards — goes stale the
moment the catalogue or the hardware moves, and duplicates the gate that already knows the answer.

So authorship supplies the ordering, which is a judgement about models and belongs to whoever
curates the manifest, and measurement supplies the filter, which is a fact about this machine and
belongs to the gate. The catalogue already writes in this voice — *the one to try first on a
modest graphics card* — and R-27 gives that sentence somewhere to be data.

## 2.8 What `statedElsewhere` was, and why it is deleted

`statedElsewhere()` maps a component id to whichever of four groups already states it, and the
Components group filters on the result — with a different rule per destination: Providers always
wins, `local-models` only wins once the model list has arrived, and the rest only win once the
component is present or ready.

It is a correct piece of code solving a problem that should not exist. Components overlaps
Providers, Voice, ComfyUI and Local models because a setup component is a legitimate fact of all
five, and the fix is to decide which one owns it rather than to suppress the other four.

R-6 therefore requires deletion rather than migration, and makes its survival a **finding about
the split** rather than a piece of tidying left for later. If, once Engines exists, something
still needs suppressing, then R-1's assignment is wrong somewhere and the specification wants to
know where.

## 2.9 A failure stated twice is not a failure stated twice

R-48 and R-63 look like a contradiction of R-1 — one fact, two surfaces. They are not.

The engine's failure is stated on Engines in the engine's own words, because the reader there is
troubleshooting and the raw message is the payload.

The *capability's* unavailability is stated on Local AI, because the reader there wants to know
whether they can make a video, and `ComfyUI connection refused` does not answer that. It is a
different proposition — derived, not restated — and deriving it needs a join between engines and
capabilities.

That join is SPEC-032's, and R-49 consumes it rather than building a second one. This is
SPEC-032's own decision 1 applied to a settings screen: a fact living in two places can disagree
with itself, and a settings screen contradicting the diagnostics view costs a debugging session
to discover that the tool is the thing that is wrong.

SPEC-028 §2.3 already asked for this shape — *the primary page shows the first failing node and
its remedy; Advanced details show every node*. What it did not have was a contract for the join.
SPEC-032 now provides one, and R-49 spends it rather than hand-rolling the walk a second time.

## 2.10 What SPEC-028 said first, and the four things this revises

SPEC-028 R-1 requires a capability-organized Settings surface; §2.3 draws the primary and
advanced layouts; R-5 makes activation a dependency transaction; R-6 fixes the state vocabulary;
T-24 and half of T-25 are open. Most of this specification is that work, finally scoped to
issues.

Four things are revised, and each is an amendment rather than a silent replacement.

**One · The groups.** SPEC-028's primary layout has two groups, **Speech** and **Local
generation**, with six rows between them. This has five rows and no groups.

*Speech* names an output kind and *Local generation* names what we do to make it, so the two
headings are not the same kind of noun — and the split puts *Cloned voices* under Local
generation while *Local voices* sits under Speech, which is a true statement about our engines
and a confusing one about voices. More decisively, the five words on this screen must be the
five words on Cloud AI (R-78), because the symmetry is what makes the local/cloud boundary
legible. `Speech` has no cloud counterpart under that name; `Voice` does.

**Two · Advanced becomes a surface.** SPEC-028 R-4 collapses the machinery into a disclosure at
the foot of the capability page. That works for one engine. There are three — ComfyUI, Ollama,
Voxa — with independent lifecycles, overlapping capabilities, and one of them hosting two
providers. A disclosure holding three engine panels is a screen wearing a chevron. R-57 gives it
the rail entry it already is, and R-59 carries every control R-4 required into it, unchanged.

**Three · Fit becomes an axis.** SPEC-028 R-6 fixed a closed vocabulary and put `unsupported`
inside it, beside `downloading`. R-19 adopts that vocabulary unchanged and R-20 lifts the two
machine facts out of it. Nothing in R-6's list is removed; two of its words are simply answered
by a different owner, which is what lets a row say *downloading, and this machine will not run
it* — a sentence the closed list cannot currently form.

**Four · Language gets a row.** SPEC-028's layout has none. Ollama and the Gemma models are
already fetched by setup and already carry a purpose string; they were simply not in that
drawing.

Everything else of SPEC-028's — Voxa's boundary, the engine lifecycle, the dependency
transaction, the per-volume disk guard, the stored-versus-tested credential distinction — is
consumed unchanged.

## 2.11 What first run does not gain

A guided `Recommended · Minimal · Choose models` chooser at first run was proposed and is
deferred, not forgotten (D10).

First run currently promises, in as many words, that *nothing here requires an account, a key, a
download or a network to start*. A setup chooser is a download decision on the first screen, and
the promise is worth more than the chooser. Someone who has never seen the product finished
cannot usefully choose between three model bundles, and the cost of asking is that the first
thing Arke Studio does is ask for something.

If it is wanted, it belongs on first entry to Local AI, where the person has already asked the
question it answers.

# 3 · Decisions

**D1 — Two screens, not one (R-2, R-3).** Rejected: a single capability screen listing local and
cloud models together, so that "who does images" has one answer. It is the better-looking mock
and the worse product: the two halves share only the noun, and the merged row is half-greyed
whichever way the reader is thinking. §2.4.

**D2 — The duplicated capability vocabulary is deliberate (R-52, R-78).** The same five words
appear on both screens. Accepted as the price of a boundary with no exceptions, and the symmetry
is what makes the split legible rather than arbitrary.

**D3 — Fit and activation are separate axes (R-9, R-20).** Costs a derivation and a table. Buys
the three cases the current design cannot express: installed but unfit, fit but absent, and
downloading onto a machine that will not run it. §2.2.

**D4 — `RecipeReadiness` is consumed, not replaced (R-25).** SPEC-021 already declares one
readiness answer consumed everywhere, and it is right. Widening *it* to carry hardware fit would
have put two questions back into one word — the fault this specification exists to fix.

**D5 — SPEC-028 R-6's vocabulary is adopted, not rewritten (R-19).** Considered: a fresh state
list shaped to the new screens. Rejected — R-6 is implemented in the setup ledger and in every
component row, and a second vocabulary for the same transitions is how two screens start
disagreeing. Two of its words move owner; none of them change spelling.

**D6 — 25 percent, stated (R-13, §2.6).** Any number here is arguable. An unstated one is worse
than a wrong one, because two builds then disagree about one machine and neither is checkable.

**D7 — Disk does not make a model slow (R-13).** Considered and rejected: treating a nearly-full
volume as a `runs-slowly` cause. It is a real problem and a different one, and reporting it as
slowness would name a symptom the machine does not have.

**D8 — `unknown` is offered, never withheld (R-23, R-29).** A model whose fit could not be
measured is installable and unrecommended. Hiding it would punish a failed probe by removing a
capability; recommending it would make a claim about a machine nobody measured. SPEC-008 D12 and
SPEC-021 D15, extended rather than amended.

**D9 — One headline label, two verdicts beneath it (R-21, R-22).** `insufficient` and
`unsupported` share the word **Unsupported** and keep separate reasons and separate remedies.
Considered: a distinct label for each. Rejected — the reader scanning a list needs few buckets,
and the reader who has stopped on a row needs the distinction. The label serves the first; the
reason serves the second.

**D10 — No setup chooser at first run (§2.11).** Deferred with its reason rather than omitted. It
contradicts a promise first run currently makes, and the promise is worth more.

**D11 — Music keeps a row with nothing in it (R-43).** Considered: hiding capabilities with no
local models. Rejected — a missing row reads as a missing feature, and an empty one reads as an
honest absence. It also stops being a special case the moment a local music model exists.

**D12 — Cloud AI cannot ship alone (R-56).** A local capability default is a real, reachable
setting today. Three outcomes were available: keep it (breaks R-3), remove it and replace it
(§1.11), or remove it and leave it in force but unreachable. The third is the worst outcome
available and is refused explicitly, because it is also the one that happens by default if
nobody decides.

**D13 — `statedElsewhere` is deleted, and its survival is a finding (R-6).** Rewriting it to suit
the new rail would preserve the fault in a new shape. Deleting it makes the split testable.

**D14 — SPEC-028's groups are revised, not reinterpreted (R-40, §2.10).** The alternative was to
implement T-24 as drawn and treat the five-row shape as a later refinement. Rejected: T-24 has
been open long enough that the next implementation of it is the one that ships, and building a
vocabulary Cloud AI cannot share would cost the boundary this specification exists to draw.

**D15 — Engines is a surface, not a disclosure (R-57, §2.10).** SPEC-028 R-4's Advanced section
was written when the local plane was one engine. It is three, one of which serves two providers.
Every control R-4 named survives; only its container changes.

# 4 · Adversarial test matrix

| # | Scenario | Expected |
| --- | --- | --- |
| 1 | Model needs 12 GB VRAM, machine has 12.1 GB | `runs-slowly`, not `runs-well`; both figures stated |
| 2 | Model needs 12 GB VRAM, machine has 24 GB | `runs-well` |
| 3 | Model needs 12 GB VRAM, machine has 8 GB | `insufficient`, not `unsupported`; smaller models offered; cloud alternative named |
| 4 | Model declares `cuda`, machine reports no accelerator, VRAM ample | `unsupported`; no smaller-model remedy offered |
| 5 | VRAM probe fails, every other floor met | `unknown`; install offered; not recommended; never `insufficient` |
| 6 | Total VRAM ample, free VRAM short at dispatch | SPEC-028 R-22's unload-and-remeasure path, unchanged; fit stays `runs-well` |
| 7 | Free disk 200 MB above a 40 GB model's floor | `runs-well` — disk does not slow anything (R-13) |
| 8 | Free disk below the closure's total but above the model's own weights | Install refused before it starts, whole-closure figure stated, per-volume evidence shown |
| 9 | Model downloading onto a machine that cannot run it | Row states both: the activation state and the refusing fit. Expressible only after R-20 |
| 10 | Model installed, then the graphics card is downgraded | Activation stays `ready`; fit becomes `insufficient`; row shows **Unsupported** with the measured reason; no contradiction |
| 11 | Model fit `runs-well`, closure complete, engine dead | Activation `ready`; the capability states unavailable from the finding, not from a second join |
| 12 | Model turned off in Providers | Shown as turned off; not reclassified as unavailable, unsupported or missing |
| 13 | Kokoro unavailable, whisper.cpp ready | Dictation reads usable; the Voice row does not collapse to one failed state (R-41, SPEC-028 R-2) |
| 14 | Hardware never probed, Local AI opened for the first time | Header states unmeasured, probe starts, no zeroes rendered at any point |
| 15 | Probe runs and fails | Header distinguishes *failed* from *not yet run* |
| 16 | Capability with no local model (`music`) | Row present, absence stated, not a fault |
| 17 | Preference order's first entry is `insufficient` here | Recommendation falls to the first `runs-well` entry; none is invented |
| 18 | No entry for a capability is `runs-well` | No recommendation; stated as an absence |
| 19 | Two models share a supporting component, one is removed | Shared component retained; the other model stays installed |
| 20 | A second declaration of a recipe's weights is added to the catalogue | Defect. R-32: weights are derived from the recipe catalogue, and two declarations drift |
| 21 | Install cancelled mid-fetch | Nothing left behind, or what was left is reported; Downloads shows the outcome |
| 22 | Install started from a capability row, app navigated away | Downloads still shows it; the row does not lose it (R-72) |
| 23 | A cloud provider appears anywhere on Local AI | Defect. R-2, checkable by enumeration over the rendered rows |
| 24 | A local model appears anywhere on Cloud AI, including disabled | Defect. R-3, same check |
| 25 | Cloud AI ships with a local `llm` default in force | Defect. R-56: migrated or cleared with a statement |
| 26 | Local default migrated, production opens a dispatch | Picker opens on the local model; per-dispatch override still available |
| 27 | Production prefers local video, local video unavailable | Dispatch states it and offers cloud; nothing is spent silently |
| 28 | Engines lands, `statedElsewhere` still required by something | Defect, and a finding about R-1's assignment — not a suppression to reinstate |
| 29 | Local AI ships a release ahead of Engines | Defect. R-63: a fact stated nowhere for a release |
| 30 | An absolute engine executable path reaches renderer state | Defect. R-59 preserves SPEC-028 R-4's host-owned path rule |
| 31 | Capability named `Clips` or `Speech` anywhere after the rename | Defect. R-78 |
| 32 | A row carries an adjective — "fast", "good reasoning" | Defect. R-76 |
| 33 | Two builds, same machine, different `runs-slowly` verdicts | Defect. R-18: the margin is a contract constant |
| 34 | Harness stated on both Engines and Harness | Defect. R-5, R-62 |
| 35 | Model detail opened | Engine named there and nowhere on the row (R-45) |

# 5 · Issue map

Epic: **#564** — Settings speaks in capabilities.

| Issue | Covers | Sections |
| --- | --- | --- |
| #565 | Fit verdicts, the headroom margin, the declared requirements, and the recommendation | §1.4, §1.6, §2.2, §2.5, §2.6 |
| #566 | Local AI: five rows, the fit/activation projection, the machine header, capability-shaped refusals | §1.5, §1.8, §2.3, §2.4, §2.9 |
| #567 | Cloud AI: rename, cloud-only filter, shared vocabulary, agent overrides to Harness | §1.9 |
| #568 | Engines: the machinery as a surface, and the deletion of the suppression | §1.10, §2.8 |
| #569 | Declared closures, whole-closure arithmetic, remove, and one Downloads surface | §1.7, §1.12 |
| #570 | Where work runs: per capability, per production, honoured or stated | §1.11 |

#565 ships alone and is useful alone; it is the spine #566 reads. #566 and #568 are sequenced by
R-63 and ship together or adjacently. #567 is independent of the local work and gated only by
R-56 — it needs #570 or the migration. #569 is an independent consumer of #565's declared
requirements. §1.13 binds every issue with a surface.

**SPEC-028 tasks absorbed.** T-24 is superseded by #566 and #568 together. T-25's outstanding
half — the Speech rows — is superseded by #566, which replaces the Speech group entirely; the
ComfyUI half of T-25 is already done and is carried forward unchanged by R-72.
